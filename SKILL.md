---
name: handoff
description: 对话换窗交接管理。长会话出现漂移信号（脱离既定交流语言、节奏明显变慢、单轮改动范围异常扩大、上下文接近上限）或阶段完成需要换新窗口时，在旧会话中生成交接文档；新窗口接手时读取最新交接文档继续工作；里程碑确认后更新存档。任何情况下只提醒、不自动执行切换，由用户决定是否换窗。
---

# handoff — 对话换窗交接

## 背景与目标用户

本 skill 面向「非专业程序员 + Vibe Coding」的用户。

- **用户画像**：有明确产品意图、但缺乏工程背景。习惯用自然语言（常是模糊说明与精确指令混合）与 AI 多轮对话迭代推进；以视觉与运行结果、而非代码质量作为验收标准；不愿也不会手工维护复杂的工程流程（写状态文档、复制粘贴、组织交接 prompt 都属于这一类）。
- **核心痛点**：长对话必然退化——上下文漂移、响应变慢、早期指令被稀释，表现为脱离既定交流语言、单轮改动范围扩大、反复需要重复说明。而换窗交接偏偏要求"把当前状态写清楚"，这正是非程序员最难承担、也最容易中断的一步。
- **本 skill 的职责**：把"换窗"的十几步操作压成一句话 `/handoff`。采集、提炼、模板生成全部由 skill 完成，用户只负责确认。
- **接收者不限**：交接文档是存放在项目磁盘上的纯 Markdown，任何在项目文件夹内工作的会话、任何 AI agent / 工具（Claude Code、Codex、其他窗口等）、或任何人（团队成员、专业开发者）都能读取接手——机制与「谁接手」无关，跨人、跨工具、跨设备都成立。

### 设计哲学（已确认，不可违背）

1. **文档是硬盘，上下文是工作台**：仓库（文件系统）始终完整，工作台（对话）始终清爽。交接靠文档，不靠 AI 的记忆。
2. **尽可能不切换**：换窗打断思维连续性，是最后手段而非常规操作。时机由「健康信号 + 里程碑」从严判定，不预设轮数；skill 只提醒、不自动执行。
3. **系统承担复杂性**：用户只确认，不复制粘贴，不写交接 prompt。
4. **地图不是领土**：交接文档只告诉新会话"去哪里读什么"（文件索引），不搬运全部文件内容。
5. **信息五要素**：git 真相（分支/提交/未提交改动）、在途工作、关键决策与坑、运行状态、下一步。

## 适用场景（以场景为主）

- **换窗续接**：对话太长或变慢时，换新窗口接着做，信息不丢。
- **跨设备续接**：这台电脑做到一半，换台电脑继续，交接文档随项目走。
- **跨工具切换**：在 Claude、Codex 等不同 AI 工具之间切换，交接文档都能被接手。
- **阶段交接**：开发做完，把项目连同交接文档交给验收/测试环节（人或 AI），写明下一步要验证什么。

## 用户协作信息（普通人 vibe coding 最珍贵的资产）

对非专业用户，接手窗口除了项目信息，还必须掌握「用户的能力画像与协作偏好」——用户会什么、需要 AI 帮什么、喜欢怎么配合、怎么判断做对了。没有它，接手窗口容易犯三类错：用技术术语汇报（用户看不懂）、自作主张改超出预期的东西、把技术决策推给做不了的用户。

- **能力画像**：用户能做什么（如懂产品/视觉/需求，不懂代码/报错/命令行），需要 AI 替做什么——决定怎么解释、怎么分工。
- **协作偏好**：用户喜欢怎么配合（先给方案确认再动手 / 直接做；结论先行；不堆技术细节；按什么标准判断做对）——决定交互方式。

**分层来源**（接手时合并使用）：
1. 跨项目通用（能力画像 + 通用偏好）→ 查用户级记忆；无现成记录时从对话与项目规则推断。
2. 项目/会话特定（本项目验收标准、本会话新确认偏好）→ 交接文档「工作方式要求」节。

模式 A 生成交接文档时提炼项目级部分；模式 B 接手时查用户级部分并合并。

## 红线（最高优先级）

1. **只提醒，不自动**：绝不自动执行换窗；信号满足时先报告，由用户决定。⚠ **用户主动输入 `/handoff` 即视为已决定换窗**，直接执行模式 A，不再询问。
2. **切换条件从严**：硬信号 ≥2 项，或硬信号 ≥1 项 + 软信号 ≥1 项，才提醒。
3. **命名统一**：交接文档放项目内 `Handoffs/` 文件夹，命名 `序号_版本号_handoff.md`（如 `01_v0.10_handoff.md`），序号递增，永不覆盖删除；标题与文件名统一使用 `handoff`。⚠ 用复数 `Handoffs/` 而非 `Handoff/`：macOS 文件系统大小写不敏感，单数会与 skill 名 `handoff/` 撞目录。
4. **交接文档 ≤100 行**；不贴对话转录原文；git 状态用 `git diff --stat` 不用全文；文件索引表而非完整文件树。
5. **密钥红线**：真实密钥、token、密码一律不写入交接文档。
6. **归属清晰**：项目决策写交接文档；只有跨项目通用的偏好才写入用户级记忆，避免记忆被项目细节污染。
7. 默认使用用户的交流语言；代码、commit message 用英文。
8. **接手目标由用户选择**：接手时只扫描文件夹名称，**用可点击的选择列表**列出所有含 `Handoffs/` 的项目，由用户点选要 handoff 哪个；**选定前不读任何项目详情，绝不自行决定、不预选目标项目**。

## 输出纪律（用户是非专业程序员，结论优先）

- 所有用户可见输出：**结论先行**，一两句说清"现状 + 建议"，不展开命令、diff、日志等技术细节。
- 技术动作（git 命令、文件扫描、测试）只作为内部手段，不向用户展示命令原文与过程。
- 用大白话；确需提及技术概念时附一句人话解释。
- 操作完直接报"已完成 + 结果 + 下一步"，不逐条汇报过程步骤。
- 交接文档里同样用大白话写"项目速览"与"关键决策"，避免堆术语。
- **一次回复只做一步**：模式 B 定位只扫描文件夹名称、列出项目列表后即停止本轮，等用户选定目标才开始读取详情；勿在同一回复内扫描+读完全部项目（输出会超限）。

## 信号定义

- **硬信号**（漂移证据）：① 上下文接近上限（约 80% 以上）；② 连续 ≥2 轮脱离既定交流语言；③ 节奏显著变慢（提交间隔或响应时长成倍增加，需用户确认）；④ 单轮改动文件数显著超出任务需要（如 >5 个）；⑤ 用户重复说明同一件事 ≥2 次。
- **软信号**（自然节点）：① 里程碑/阶段完成且验证结论已写回；② 用户主动表示收工或换任务。

**提醒规则**：仅软信号 → 不提醒。硬信号 ≥2 项，或硬信号 ≥1 项 + 软信号 ≥1 项 → 在回复末尾一句话提醒（列出观察到的信号 + 两个选项：继续压缩 / 换窗交接），由用户选择。任何情况下不自动执行。

## 场景判断

按会话身份直接判定，**不弹任何"想做什么"的选择**：

- **本会话不是新窗口首次消息**（已有工作历史）→ **模式 A**：直接执行采集与生成（`/handoff` 命令本身即代表用户已决定换窗），不询问意图
- **本会话是新窗口首次消息** → 模式 B：列出含交接文档的项目供用户点选
- **仅当用户口头明确要求**"存档 / 更新交接文档"→ 模式 C

## 模式 A · 切换前（旧会话内）

**执行方式：全程静默，直到需要用户拍板才出现。** 体检、采集、提炼过程不向用户播报任何"开始采集、正在整理"之类的过程话语；用户唯一会看到的是步骤 4 的一次点选弹窗（两个多选问题），以及步骤 8 的简洁收尾。

1. **体检（内部，不播报）**：扫过本会话消息量、漂移症状、上下文余量。无不安因素则不出现；有**不安因素**（如大量未提交改动会丢进度）才汇入步骤 8 收尾时一句提醒。
2. **采集**（用户确认换窗后；内部动作，不向用户展示命令原文）：
   - git 状态（分支/最新提交/未提交改动）、运行中的服务、最近测试结果
   - 对未提交改动逐个概括"改了什么、为什么"（≤2 行/文件）
   - 扫描项目根目录结构（用于生成「文件索引地图」）
3. **提炼（按三层结构）**：内容对应模板 §0 / §3+§4 / §2+§5：
   - **层 1 · 全貌**（长期稳定）：项目定位、核心思路、架构——**继承上一份交接文档的速览做增量更新**，再用 PRD/规则文件关键段落校验；无旧文档才从零总结。
   - **层 2 · 阶段**（中期）：已完成什么（引用最近 1-2 份 plan 的**验证结论**，不靠会话记忆）、最近聚焦什么、按总体计划下一步可能是什么；plan 缺失则降级"git 提交 + 交接文档"推测。
   - **层 3 · 交接**（短期即时）：进度对齐、关键决策与坑（≤7 条，每条 1-2 行，写"为什么"不只写"是什么"）、注意事项、工作环境状态、本会话**新确认的用户偏好与验收标准**。
4. **唯一确认（一次弹窗、两个多选问题）**：层 2/层 3 的备选项由 skill 自动提炼，用 **AskUserQuestion（multiSelect，可复选多项）** 一次问完，均留「其他」供补充；环境不支持点选则列编号、用户回编号：
   - **问 1（层 2）**：按计划下一步走哪个方向？候选 2-3 个（如"验收后发布 vX.Y"、"继续打磨某处"、"收尾标记某事"）。
   - **问 2（层 3）**：本次交接有哪些事项需要先关注/拍板？候选 = 具体待办与未决点。
   **其余事项一律不询问**：层 1 全貌 → 直接写入；新确认的偏好/验收标准 → 提炼后直接写入；描述用词 → 不提。用户补充内容由 skill 提炼 ≤2 行写入，不要求用户组织语言成文档。
5. **写交接**：按 `templates/handoff-template.md` 生成到项目根 `Handoffs/`（最大序号 +1）。交接文档含三层内容（§0 层 1 / §3+§4 层 2 / §2+§5 层 3）。生成后如项目有 git 仓库，commit 该交接文档——这是交接文档唯一一次提交，接手时不重复提交。
6. **规则自检**：检查项目规则文件（CLAUDE.md 体系）是否齐全；缺则按 `templates/project-rules-template.md` 补建（是否补建用 AskUserQuestion 点选确认）。
7. **冲突检查**：查同仓库是否有其他运行中的会话；有则提醒用户先停旧再开新。
8. **简洁收尾**（不输出模式化换窗清单，只报 2-4 行要点）：
   - 交接准备已完成 + 交接文档路径（相对项目根）
   - **特别注意**（仅当存在才列）：未提交改动未处理、其他会话仍在运行等**会丢进度或冲突**的事项；一般小事不列
   - 结束语（一行）："新窗口输入 /handoff 选择本项目即可接手。"

## 模式 B · 接手（新会话内）

**定位规则**（第一步：只扫文件夹名 → 列可点击列表 → 等用户选择，本轮到此为止）：
1. **只扫描文件夹名称，不读任何文件内容**：从当前工作目录逐级向父目录走（最远到用户主目录），每一级都用目录名扫描（`find -maxdepth 4 -type d -name "Handoffs"`），合并去重——**项目无论嵌套几层都能收集全**（如 `项目/子项目/子项目/Handoffs`），输出量恒定很小。
2. 呈现选择列表：用 **AskUserQuestion 工具渲染可点击选项**（每个选项 = 一个项目），用户点击即选，**无需打字**。选项 label 用简短项目名，description 写完整名与交接档数量（如"含 2 处交接档"）。**绝不自行决定、不预选、不推荐默认项。**
   - 候选超过 4 个（AskUserQuestion 上限）→ 编号列表，用户回复编号即可（仍不用打项目全名）。
   - AskUserQuestion 不可用（其他工具环境）→ 同上，编号列表 + 回复编号。
3. **停止本轮**：列完列表即结束回复，不读取任何项目详情；等用户选定目标项目后，才进入下面"定位完成后"的读取流程。
4. 一个都找不到 → 报告"未发现任何含交接文档的项目"，询问用户提供项目路径（用户直接粘贴路径即可）。

目标项目选定后（**接手动作自行执行，不逐项询问**；读取只针对选定项目，不扫描其他项目）：
1. 读目标项目 `Handoffs/` 中最大序号文档的三层内容：先读 §0（层 1 全貌）重建心智模型，再读 §3+§4（层 2 阶段）、§2+§5（层 3 交接）。
2. **必读 PRD/需求文档**校验层 1 全貌（用户要求，不可跳过）；按「文件索引地图」定位后续要读的文件。
3. **查用户级协作记忆**（固化流程）：读取能力画像与跨项目协作偏好（结论先行、先方案再动手、不堆技术细节等），与交接文档里的项目级协作信息合并，作为本会话对用户的协作方式。
4. 读规则文件与相关计划（只读所列文件，不全盘扫描）——用以核对层 2 阶段结论。
5. **查看最近 5 次 git 提交**：与层 2/层 3 描述互证"前面在做什么、进度到哪、接下来方向"；不一致说明差异。
6. 把文档头部状态由「待接手」更新为「已接手（日期）」——直接执行，不询问。**不 commit**（交接文档在模式 A 生成时已提交，接手无实质内容变更）。
7. **向用户复述三层 + 一次多选开工**（全程大白话）：
   - **层 1 · 全貌**：项目是做什么的（1 句，来自文档速览，PRD 校验后）
   - **层 2 · 阶段**：现在到哪个阶段、最近聚焦什么、按计划下一步方向是什么（1-2 句，git 提交与 plan 互证）
   - **层 3 · 交接**：最要紧的一条注意/坑（1 句）
   然后 **AskUserQuestion（multiSelect）** 问「接下来先做哪件？」——候选 = 计划下一步方向 + 交接文档具体待办，可多选 + 「其他」；用户点选即开工，不再单独问"可以开始吗"。环境不支持点选则列编号、用户回编号。

## 模式 C · 存档（里程碑确认后）

1. 定位 `Handoffs/` 最新文档；无则按模式 A 生成。
2. 直接更新三处：已完成项（附验证结论）、下一步、关键决策与坑（如新增）——不询问。
3. 向用户报结论：存档完成 + 更新了什么；顺带一句"验证结论是否同步写回对应计划文档"。行数超限则压缩旧内容而非删除。

## 模板

- `templates/handoff-template.md` — 模式 A 输出格式（交接文档模板）
- `templates/project-rules-template.md` — 模式 A 步骤 6 补建规则文件的格式
